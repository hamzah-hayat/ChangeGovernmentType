namespace = cgt_events

# Decisions:
# decision_change_government_type, by Hamzah

# This is the popup screen that we see 
cgt_events.0001 = {
	type = character_event
	title = cgt_events.0001.t
	desc = cgt_events.0001.desc
	theme = realm
	override_background = { reference = courtyard }
	left_portrait = {
		character = root
		animation = storyteller
	}

	immediate = {
		if = {
			limit = {
				has_character_flag = ready_to_receive_seed
			}
			legend_seed_great_deed_dynasty_effect = yes
			remove_character_flag = ready_to_receive_seed
		}
	}

	option = {
		name = cgt_events.0001.feudal
		convert_whole_realm_to_feudal_effect = yes
	}

	option = {
		name = cgt_events.0001.clan
		convert_whole_realm_to_clan_effect = yes
	}

	option = { 
		trigger = {
			has_tgp_dlc_trigger = yes
		}

		name = cgt_events.0001.feudal_japan
		convert_whole_realm_to_feudal_japan_effect = yes
	}

	option = {
		name = cgt_events.0001.cancel
		add_prestige_no_experience = 200
	}
}